#include <iostream>
#include <vector>
using namespace std;

void kill_process(vector <vector<int>>)

int main()
{
	vector <vector<int>> graph(nodes);

	vector <int> pid;
	vector <int> ppid;

	for (int i = 0; i < edges; i++)
	{
		int pid_, ppid_;
		cin >> pid_ >> ppid_;

		pid.push_back(pid_);
		ppid.push_back(ppid_);
	}


}